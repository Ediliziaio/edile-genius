

# Fix Navbar & Footer Logo

## Changes

### 1. Navbar — Remove "AI per l'Edilizia" badge and clean up layout
- Remove the `<span>` badge (lines 86-88)
- Change `flex flex-col` to just a simple link with the logo image
- Keep `h-8` for proper sizing

### 2. Footer — Increase logo size
- Change `h-7` to `h-10` on the footer logo image

